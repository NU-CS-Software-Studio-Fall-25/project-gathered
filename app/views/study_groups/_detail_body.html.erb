<div class="rounded-2xl border border-white/15 bg-white/10 backdrop-blur-2xl shadow-2xl p-5">
  <div class="flex items-start justify-between mb-4">
    <h1 class="text-2xl font-bold text-white"><%= study_group.topic %></h1>
    <span class="inline-flex items-center rounded-md px-2.5 py-1 text-xs font-semibold <%= study_group.status == 'upcoming' ? 'bg-green-500/20 text-green-300 border border-green-500/30' : study_group.status == 'ongoing' ? 'bg-blue-500/20 text-blue-300 border border-blue-500/30' : 'bg-slate-500/20 text-slate-300 border border-slate-500/30' %>">
      <%= study_group.status.titleize %>
    </span>
  </div>

  <div class="grid grid-cols-1 lg:grid-cols-3 gap-5">
    <div class="lg:col-span-2 space-y-5">
      <div class="bg-white/5 rounded-lg border border-white/10 p-4">
        <h3 class="text-base font-semibold text-white mb-3">Details</h3>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-3">
          <div class="flex items-start gap-2.5">
            <span class="text-xl">📚</span>
            <div class="flex-1">
              <strong class="block text-xs font-medium text-slate-300">Course</strong>
              <p class="text-sm text-white"><%= study_group.course.course_name %></p>
            </div>
          </div>

          <div class="flex items-start gap-2.5">
            <span class="text-xl">📅</span>
            <div class="flex-1">
              <strong class="block text-xs font-medium text-slate-300">Time</strong>
              <p class="text-sm text-white"><%= study_group.formatted_time_range %></p>
            </div>
          </div>

          <% if study_group.location.present? %>
            <div class="flex items-start gap-2.5">
              <span class="text-xl">📍</span>
              <div class="flex-1">
                <strong class="block text-xs font-medium text-slate-300">Location</strong>
                <p class="text-sm text-white"><%= study_group.location %></p>
              </div>
            </div>
          <% end %>

          <div class="flex items-start gap-2.5">
            <span class="text-xl">👤</span>
            <div class="flex-1">
              <strong class="block text-xs font-medium text-slate-300">Created by</strong>
              <p class="text-sm text-white"><%= study_group.creator.name %></p>
            </div>
          </div>

          <% if study_group.duration_minutes %>
            <div class="flex items-start gap-2.5">
              <span class="text-xl">⏱️</span>
              <div class="flex-1">
                <strong class="block text-xs font-medium text-slate-300">Duration</strong>
                <p class="text-sm text-white"><%= format_duration(study_group.duration_minutes) %></p>
              </div>
            </div>
          <% end %>
        </div>

        <% if study_group.description.present? %>
          <div class="mt-4 pt-3 border-t border-white/10">
            <strong class="block text-xs font-medium text-slate-300 mb-1.5">Description</strong>
            <p class="text-sm text-white break-words"><%= study_group.description %></p>
          </div>
        <% end %>
      </div>
    </div>

    <div class="lg:col-span-1">
      <%= render "study_groups/members_list", study_group: study_group, members: members %>
    </div>
  </div>

  <div class="mt-5 pt-4 border-t border-white/10">
    <%= render "study_groups/detail_actions", study_group: study_group %>
  </div>
</div>

